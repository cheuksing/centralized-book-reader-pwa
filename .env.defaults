# Shared local development defaults for the PWA and Worker.
WORKER_ORIGIN=http://localhost:5173
WORKER_ACCESS_TOKEN=dev-only-token
WORKER_ALLOWED_ORIGINS=http://localhost:5173

# Set these in .env.local locally or GitHub Actions secrets/variables for deployment.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
